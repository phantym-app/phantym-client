import { h } from 'preact';
import styles from './Settings.module.scss';
import classnames from 'classnames';

const Settings = () => {
  return <div className={classnames(styles.root)}>Hi this is Settings</div>;
};

export default Settings;
